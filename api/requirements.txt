# AI Hygiene Backend — Python Dependencies
# Install with: pip install -r requirements.txt
#
# For CPU-only PyTorch (smaller download, ~200MB vs 2GB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then run the rest:
#   pip install -r requirements.txt
#
# For CUDA (NVIDIA GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Hugging Face — transformers + model download
transformers>=4.40.0
huggingface-hub>=0.20.0
tokenizers>=0.15.0
safetensors>=0.4.0

# PyTorch — needed for ScamLLM (no ONNX export) and heavy generative models
# CPU wheel (default):
torch>=2.1.0

# ONNX Runtime with DirectML (AMD NPU / any DirectX 12 GPU — Windows only)
onnxruntime-directml>=1.17.0

# Optimum — ONNX export & ORT model wrappers
optimum[onnxruntime]>=1.18.0

# Accelerate — required for device_map="auto" in heavy model loading
accelerate>=0.25.0

# Requests / networking
requests>=2.28.0
aiohttp>=3.8.0
